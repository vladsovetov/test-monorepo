{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "syncpack": "^8.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "repository": "https://github.com/vladsovetov/test-monorepo",
  "scripts": {
    "syncversions": "syncpack fix-mismatches -f shared",
    "release": "cd packages/shared && yarn test && changeset publish",
    "test": "console.log('tests')"
  },
  "dependencies": {
    "@changesets/cli": "^2.23.0"
  }
}
